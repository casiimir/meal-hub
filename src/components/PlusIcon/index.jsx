import React from 'react';
import PlusIcon from '..PlusIcon';

const FridgeButton = () => {
  return (
    <div>
      <ButtonIcon isRound={true} icona={<PlusIcon />} />
    </div>
  );
};

export default FridgeButton;
